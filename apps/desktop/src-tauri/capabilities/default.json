{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window — core APIs + native dialog (used by Settings → Plugins import).",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
